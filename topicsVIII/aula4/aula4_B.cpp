#include <bits/stdc++.h>
using namespace std;

using ull = unsigned long long int;

int main(){

    int t;
    cin >> t;

    ull n, k;
    for (int i = 0; i < t; i++){
        cin >> n >> k;

        if (k < n)
            cout << k << '\n';

        
    }

    return 0;
}